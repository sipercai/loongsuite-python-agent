# Do not edit this file.
# This file is generated automatically by executing tox -e generate-workflows

name: Lint 0

on:
  push:
    branches-ignore:
    - 'release/*'
    - 'otelbot/*'
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

env:
  # Set the SHA to the branch name if the PR has a label 'prepare-release' or 'backport'
  # otherwise, set it to the pinned 0.62-compatible core commit.
  # For PRs you can change the inner fallback if needed.
  # For pushes you change the outer fallback if needed.
  # The logic below is used during releases and depends on having an equivalent branch name in the core repo.
  CORE_REPO_SHA: ${{ github.event_name == 'pull_request' && (
       contains(github.event.pull_request.labels.*.name, 'prepare-release') && github.event.pull_request.head.ref ||
       contains(github.event.pull_request.labels.*.name, 'backport') && github.event.pull_request.base.ref ||
       '37dea4bbdb1a3c83b96fc22c2f68a848b4989fb5'
    ) || '37dea4bbdb1a3c83b96fc22c2f68a848b4989fb5' }}
  CONTRIB_REPO_SHA: main
  PIP_EXISTS_ACTION: w

jobs:

  batch_001_tox:
    name: tox batch 001 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-openai-v2,lint-instrumentation-openai_agents-v2,lint-instrumentation-vertexai,lint-instrumentation-google-genai

  batch_002_tox:
    name: tox batch 002 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-anthropic,lint-instrumentation-claude-agent-sdk,lint-resource-detector-containerid,lint-resource-detector-azure

  batch_003_tox:
    name: tox batch 003 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-sdk-extension-aws,lint-distro,lint-opentelemetry-instrumentation,lint-instrumentation-aiohttp-client

  batch_004_tox:
    name: tox batch 004 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-aiohttp-server,lint-instrumentation-aiopg,lint-instrumentation-aws-lambda,lint-instrumentation-botocore

  batch_005_tox:
    name: tox batch 005 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-boto3sqs,lint-instrumentation-django,lint-instrumentation-dbapi,lint-instrumentation-boto

  batch_006_tox:
    name: tox batch 006 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-asyncclick,lint-instrumentation-click,lint-instrumentation-elasticsearch,lint-instrumentation-falcon

  batch_007_tox:
    name: tox batch 007 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-fastapi,lint-instrumentation-flask,lint-instrumentation-urllib,lint-instrumentation-urllib3

  batch_008_tox:
    name: tox batch 008 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-requests,lint-instrumentation-starlette,lint-instrumentation-jinja2,lint-instrumentation-logging

  batch_009_tox:
    name: tox batch 009 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-exporter-richconsole,lint-exporter-prometheus-remote-write,lint-instrumentation-mysql,lint-instrumentation-mysqlclient

  batch_010_tox:
    name: tox batch 010 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-psycopg2,lint-instrumentation-psycopg,lint-instrumentation-pymemcache,lint-instrumentation-pymongo

  batch_011_tox:
    name: tox batch 011 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-pymysql,lint-instrumentation-pymssql,lint-instrumentation-pyramid,lint-instrumentation-asgi

  batch_012_tox:
    name: tox batch 012 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-asyncpg,lint-instrumentation-sqlite3,lint-instrumentation-wsgi,lint-instrumentation-grpc

  batch_013_tox:
    name: tox batch 013 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-sqlalchemy,lint-instrumentation-redis,lint-instrumentation-remoulade,lint-instrumentation-celery

  batch_014_tox:
    name: tox batch 014 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-system-metrics,lint-instrumentation-threading,lint-instrumentation-tornado,lint-instrumentation-tortoiseorm

  batch_015_tox:
    name: tox batch 015 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-httpx,lint-util-http,lint-exporter-credential-provider-gcp,lint-util-genai

  batch_016_tox:
    name: tox batch 016 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-propagator-aws-xray,lint-propagator-ot-trace,lint-instrumentation-sio-pika,lint-instrumentation-aio-pika

  batch_017_tox:
    name: tox batch 017 (4 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 4 -e lint-instrumentation-aiokafka,lint-instrumentation-kafka-python,lint-instrumentation-confluent-kafka,lint-instrumentation-asyncio

  batch_018_tox:
    name: tox batch 018 (2 envs)
    runs-on: loongsuite-python-agent-fork-arc
    timeout-minutes: 30
    steps:
      - name: Checkout repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox run-parallel -p 2 -e lint-instrumentation-cassandra,lint-processor-baggage
